name: xmake buildnnon:n  workflow_dispatch:nnjobs:n  build:n    name: build-linuxn    runs-on: ubuntu-latestnn    steps:n    - name: install dependenciesn      run: |n        sudo apt-get updaten        sudo apt-get install -y g++ libssl-dev libgtest-devnn    - name: setup xmaken      uses: xmake-io/github-action-setup-xmake@v1n      with:n        xmake-version: latestnn    - name: pull coden      uses: actions/checkout@v2nn    - name: xmaken      run: |n        xmake -rn        xmake -g testn        xmake -g tutorialn        xmake -g benchmarknn    - name: run sharedn      run: |n        xmake f -k sharedn        xmake -rn        xmake -g testn        xmake -g tutorialn        xmake -g benchmarkn